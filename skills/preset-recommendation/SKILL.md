# Preset Recommendation Skill

## Purpose
Nominate correction presets that match diagnosed issues.

## Boundary
The skill proposes. Deterministic engine code validates visibility, compatibility, thresholds, and composite rules.
