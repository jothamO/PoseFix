# Pose Diagnosis Skill

## Purpose
Separate the current pose into `preserve`, `adjust`, and `avoid`.

## Rule
Prefer the minimum necessary correction. A mechanically good element is a preservation anchor, not an invitation to redraw it.
